# =============================================================================
# Super Mario Bros Neuroevolution - Dependencies
# =============================================================================
# IMPORTANT: gym must be pinned to 0.25.2. Newer versions of Gym (>=0.26)
# changed the step() return signature from 4-tuples to 5-tuples and broke
# compatibility with the nes-py emulator backend.
# =============================================================================

gym==0.25.2
gym-super-mario-bros
nes-py
numpy<2
opencv-python==4.10.0.84

# CuPy for GPU Acceleration on NVIDIA GPUs
# Note: This specifies the CUDA 12.x wheel. If you have CUDA 11.x, change this to cupy-cuda11x
cupy-cuda12x